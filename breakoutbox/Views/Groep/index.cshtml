@model Sessie
@{
    ViewData["Title"] = Model.Naam;
}


<p>@ViewData["Title"]</p>
<h2>Groepen</h2>
<table class="table">
    <tr>
        <th>Naam</th>
        <th>Klas</th>
        <th>Contactleer</th>
        <th></th>
    </tr>
    @foreach (var groep in Model.SessieGroep)
    {
        <tr>
            <td>@groep.SessieNaam</td>
            <td>@groep.SessieNaam</td>
            @*<td><input type="checkbox" @(groep.SessieNaam ? "checked" : "") /></td>*@
            <td><a asp-controller="Groep" asp-action="Start" asp-route-id="@groep.SessieNaam">Start</a></td>
        </tr>
    }
</table>
<a asp-controller="Sessie" asp-action="Index">Back</a>